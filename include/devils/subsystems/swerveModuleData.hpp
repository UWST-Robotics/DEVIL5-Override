#include "./devilLib/devils.h"

namespace devils
{
    struct SwerveModuleData {
        // Stores info about swerve modules (mostly calibration angles)
        // Each module is physically labelled starting from zero, these are the corresponding properties each module has
        // Will need to be changed if a module is swapped from a front to a rear or its encoder gets changed
        // Calibration angle should remain the same as long as the "hat" with the magnet doesn't change

        
    }
}